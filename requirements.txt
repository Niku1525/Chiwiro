py-cord>=2.6.0
yt-dlp>=2024.8.6
PyNaCl>=1.5.0
python-dotenv>=1.0.0
requests>=2.31.0

# Para la app de escritorio (chiwiro_app.py)
Pillow>=10.0.0
psutil>=5.9.0

# Solo para los scripts de tools/ (acceso directo y el arranque automatico.
# y arranque automatico). El bot y la app no lo necesitan.
pywin32>=306
